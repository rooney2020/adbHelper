# Changelog

## [v0.0.2] - 2026-06-24

### Added

- **编译环境管理**：按类型（AOSP、QNX、APP）配置源码路径、Make 参数、并行任务数，一键启动编译并查看实时日志
- **编译目标预设**：每个环境可添加多个编译目标，每个目标独立配置 make 参数和推包文件列表
- **目标编译**：点按编译使用预设目标的 make 参数替代环境默认参数
- **一键推包**：编译目标可配置多个推包文件（本地产物路径 → 设备路径），一键执行 `adb push`（多文件顺序推送）
- **推包日志**：每个目标的推包过程独立显示实时日志，支持中断
- **Make 参数编辑**：环境卡片上直接编辑 Make 参数和并行任务数，无需打开对话框
- **编译环境导入/导出**：BuildPage 页头「导入」「导出」按钮，支持独立导入导出编译环境配置
- **全局导入导出**：右上角全部导出/导入现在包含编译环境数据，导入后自动刷新页面
- **编译结果展示**：编译完成后显示用时和退出码，失败时高亮警告

### Changed

- 推包路径自动拼接源码目录：`out/target/...` 自动解析为 `{sourceDir}/out/target/...`
- 全局导入校验放宽：兼容不含编译环境的旧导出文件

## [v0.0.1] - 2026-06-10

### Added

- 项目初始化
- ADB 设备管理（连接 / 断开 / 信息查看）
- 命令面板系统
- 快捷命令目录
- Logcat 实时日志查看与过滤
- 性能分析页面（CPU / 内存 / 网络 / GPU / 磁盘 / FPS 等）
- Monkey 测试页面
- 按键模拟页面
- 编排宏任务
- Dumpsys 页面
- 布局查看器
- 备份与恢复
- 数据导入导出
